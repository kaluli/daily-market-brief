{
  "name": "daily-market-brief",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm run build && cp -r .next ../.. && (cd ../../api && GOFLAGS=-mod=readonly go build -v -o /dev/null ./index.go)",
    "install": "cd apps/web && npm install"
  }
}
